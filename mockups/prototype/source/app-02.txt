  function renderMore(){return `${topbar('More','Explore Nudge',false,true)}${[['gemini','Ask Gemini','Natural-language actions'],['history','History','Completed tasks and list sessions'],['insights','Insights','Progress without pressure'],['templates','Templates','Starter chores and lists'],['widgets','Widgets','Home-screen layout previews'],['archived','Archived items','Restore removed content'],['onboarding','Preview onboarding','Review the setup experience'],['settings','Settings','Interventions, lists, and data']].map(([r,t,s])=>`<div class="link-row" data-route="${r}"><div><strong>${t}</strong><span style="display:block;margin-top:4px">${s}</span></div><span>›</span></div>`).join('')}`}
  function renderTaskDetail(){const t=taskById(state.completionTarget)||taskById(state.focusTask)||state.tasks[0];return `${topbar(t.title,[t.area,t.room].filter(Boolean).join(' › '),false,true)}<div class="card"><div class="card-head"><div><div class="section-title">${t.type==='chore'?'Recurring chore':'One-time task'}</div><div class="quick-title" style="color:var(--ink);margin-top:10px">${esc(t.title)}</div></div><span class="chip ${t.due==='overdue'?'danger':t.due==='today'?'warn':''}">${esc(t.due)}</span></div><div class="divider"></div><div class="link-row"><strong>Estimated time</strong><span>${t.estimate} minutes</span></div><div class="link-row"><strong>Recurrence</strong><span>${esc(t.recurrence||'Does not repeat')}</span></div><div class="link-row"><strong>Completion grading</strong><span>${t.grading?'Enabled':'Not needed'}</span></div><div class="link-row"><strong>Include in nudges</strong><span>${t.nudge?'Yes':'No'}</span></div></div><button class="button primary block" data-start-task="${t.id}">Start ${t.type==='chore'?'chore':'task'}</button><div class="spacer"></div><button class="button soft block" data-complete-task="${t.id}">Mark complete</button>`}

  function render(){clearInterval(focusInterval);nav.innerHTML=navHtml();nav.style.display=['intervention','focus','room-reset','onboarding'].includes(state.route)?'none':'flex';document.querySelector('.fab').style.display=['intervention','focus','room-reset','shopping','onboarding'].includes(state.route)?'none':'block';const pages={today:renderToday,areas:renderAreas,'area-detail':renderAreaDetail,room:renderRoom,tasks:renderTasks,lists:renderLists,'list-detail':renderListDetail,shopping:renderShopping,intervention:renderIntervention,focus:renderFocus,'room-reset':renderRoomReset,gemini:renderGemini,history:renderHistory,insights:renderInsights,templates:renderTemplates,settings:renderSettings,search:renderSearch,more:renderMore,onboarding:renderOnboarding,widgets:renderWidgets,archived:renderArchived,'task-detail':renderTaskDetail};screen.innerHTML=(pages[state.route]||renderToday)();screen.scrollTop=0;bindDynamic();if(state.route==='focus')startFocusTimer();}

  function bindDynamic(){const listInput=document.getElementById('list-add-input');if(listInput){listInput.addEventListener('input',()=>renderSuggestions(listInput.value));setTimeout(()=>renderSuggestions(''),0);}const search=document.getElementById('global-search');if(search)search.addEventListener('input',()=>renderSearchResults(search.value));}
  function renderSuggestions(query){const box=document.getElementById('list-suggestions');if(!box)return;const l=listById(state.selectedList),q=query.trim().toLowerCase();const suggestions=(l.catalog||[]).filter(x=>!q||x.name.toLowerCase().includes(q)).slice(0,4);box.innerHTML=suggestions.length?`<div class="suggestion-box">${suggestions.map(x=>`<div class="suggestion-row" data-add-catalog="${esc(x.name)}"><div><div class="task-title">${esc(x.name)}</div><div class="task-sub">Last added ${esc(x.lastUsed)}</div></div><strong>+</strong></div>`).join('')}</div>`:'';}
  function renderSearchResults(q){const root=document.getElementById('search-results');if(!root)return;const term=q.trim().toLowerCase();if(!term){root.innerHTML='<div class="empty"><div class="empty-icon">⌕</div><h3>Search Nudge</h3><p>Find chores, tasks, areas, lists, and remembered items.</p></div>';return;}const tasks=state.tasks.filter(t=>t.title.toLowerCase().includes(term)||t.area.toLowerCase().includes(term)||t.room.toLowerCase().includes(term));const lists=state.lists.filter(l=>l.name.toLowerCase().includes(term)||l.items.some(i=>i.name.toLowerCase().includes(term)));root.innerHTML=`<div class="section-head"><div class="section-title">Tasks and chores</div><span class="count-badge">${tasks.length}</span></div><div class="list">${tasks.map(t=>taskRow(t)).join('')||'<div class="card-sub">No task matches.</div>'}</div><div class="section-head"><div class="section-title">Lists</div><span class="count-badge">${lists.length}</span></div>${lists.map(listCard).join('')||'<div class="card-sub">No list matches.</div>'}`;}
  function formatSeconds(s){const m=Math.floor(s/60),sec=s%60;return `${String(m).padStart(2,'0')}:${String(sec).padStart(2,'0')}`;}
  function startFocusTimer(){focusSeconds=taskById(state.focusTask)?.estimate*60||180;const el=document.getElementById('focus-timer');focusInterval=setInterval(()=>{focusSeconds=Math.max(0,focusSeconds-1);if(el)el.textContent=formatSeconds(focusSeconds);if(!focusSeconds)clearInterval(focusInterval);},1000);}

  function openQuickAdd(){overlayRoot.innerHTML=`<div class="overlay"><div class="sheet"><div class="sheet-handle"></div><div class="sheet-head"><h2>What needs doing?</h2><button class="close-button" data-action="close-overlay">×</button></div><div class="field"><label>Quick entry</label><input id="quick-add-title" class="text-input" placeholder="Clean bathroom weekly" autofocus></div><div class="chips"><button class="chip active" data-quick-type="task">Task</button><button class="chip" data-quick-type="chore">Chore</button><button class="chip" data-quick-type="list">List item</button></div><div class="form-grid" style="margin-top:14px"><div class="field"><label>Area</label><select id="quick-area" class="select-input"><option>Personal</option><option>House</option><option>Car</option></select></div><div class="field"><label>Estimate</label><select id="quick-estimate" class="select-input"><option value="5">5 min</option><option value="10">10 min</option><option value="15">15 min</option><option value="30">30 min</option></select></div></div><div class="notice green">Nudge will infer common words like “weekly,” “kitchen,” “bathroom,” and grocery item names.</div><button class="button primary block" data-action="save-quick-add">Add item</button><div class="spacer"></div><button class="button ghost block" data-route="gemini" data-action="close-overlay">Ask Gemini instead</button></div></div>`;overlayRoot.dataset.quickType='task';setTimeout(()=>document.getElementById('quick-add-title')?.focus(),50);}
  function openAddArea(){overlayRoot.innerHTML=`<div class="overlay"><div class="sheet"><div class="sheet-handle"></div><div class="sheet-head"><h2>Add area</h2><button class="close-button" data-action="close-overlay">×</button></div><div class="field"><label>Area name</label><input id="new-area-name" class="text-input" placeholder="Basement" autofocus></div><div class="field"><label>Optional rooms</label><input id="new-area-rooms" class="text-input" placeholder="Storage, Utility room"></div><div class="notice green">Separate multiple rooms with commas. You can leave this blank.</div><button class="button primary block" data-action="save-area">Save area</button></div></div>`;setTimeout(()=>document.getElementById('new-area-name')?.focus(),50);}
  function openNewList(){overlayRoot.innerHTML=`<div class="overlay"><div class="sheet"><div class="sheet-handle"></div><div class="sheet-head"><h2>New reusable list</h2><button class="close-button" data-action="close-overlay">×</button></div><div class="field"><label>List name</label><input id="new-list-name" class="text-input" placeholder="Pharmacy" autofocus></div><div class="field"><label>Icon</label><select id="new-list-icon" class="select-input"><option>📝</option><option>🛒</option><option>🧺</option><option>🧳</option><option>🚗</option></select></div><div class="setting-row"><div><strong>Remember completed items</strong><small>Suggest them again later</small></div><div class="toggle on"></div></div><button class="button primary block" data-action="save-list">Create list</button></div></div>`;setTimeout(()=>document.getElementById('new-list-name')?.focus(),50);}
  function openTemplate(index){const templates=[{name:'Kitchen basics',icon:'🍽️',items:['Take out trash','Wipe counters','Clean microwave','Mop floor','Clean refrigerator']},{name:'Bathroom cleaning',icon:'🧼',items:['Clean sink','Clean mirror','Scrub shower','Replace towels']},{name:'Bedroom reset',icon:'🛏️',items:['Make bed','Put clothes away','Dust surfaces','Vacuum floor']},{name:'Monthly car maintenance',icon:'🚗',items:['Check tire pressure','Check washer fluid','Review oil level','Clean interior','Review registration','Check emergency kit']},{name:'Travel packing',icon:'🧳',items:['Passport','Phone charger','Toiletries','Medications','Clothes']},{name:'Household restock',icon:'🧺',items:['Trash bags','Paper towels','Laundry detergent','Dish soap','Cleaning spray']}];const t=templates[index]||templates[0];overlayRoot.dataset.templateIndex=String(index);overlayRoot.innerHTML=`<div class="overlay"><div class="sheet"><div class="sheet-handle"></div><div class="sheet-head"><h2>${t.icon} ${esc(t.name)}</h2><button class="close-button" data-action="close-overlay">×</button></div><p class="muted small">Review the starter items before adding them.</p>${t.items.map(x=>`<div class="setting-row"><div><strong>${esc(x)}</strong><small>Default schedule can be changed later</small></div><span class="check done"></span></div>`).join('')}<div class="spacer"></div><button class="button primary block" data-action="confirm-template">Add ${t.items.length} items</button></div></div>`;}
  function openGrade(t){overlayRoot.innerHTML=`<div class="overlay"><div class="sheet"><div class="sheet-handle"></div><div class="sheet-head"><h2>How much did you complete?</h2><button class="close-button" data-action="close-overlay">×</button></div><p class="muted small">${esc(t.title)}</p><div class="grade-options"><button class="grade-option" data-grade="Light" data-task="${t.id}"><strong>Light</strong><span>Quick reset or surface cleaning</span></button><button class="grade-option" data-grade="Moderate" data-task="${t.id}"><strong>Moderate</strong><span>Normal, thorough cleaning</span></button><button class="grade-option" data-grade="Deep" data-task="${t.id}"><strong>Deep</strong><span>Detailed full cleaning</span></button></div></div></div>`;}
  function openNotNow(){overlayRoot.innerHTML=`<div class="overlay"><div class="sheet"><div class="sheet-handle"></div><div class="sheet-head"><h2>Not now</h2><button class="close-button" data-action="close-overlay">×</button></div>${['Remind me in 10 minutes','Remind me in 30 minutes','Pause for 1 hour','Pause until tomorrow','This app is needed right now'].map(x=>`<div class="link-row" data-action="snooze-choice" data-choice="${x}"><strong>${x}</strong><span>›</span></div>`).join('')}<div class="divider"></div><div class="link-row" data-action="return-previous-app"><strong>Return to Instagram</strong><span>›</span></div></div></div>`;}
  function completeTask(id,grade='Completed'){const t=taskById(id);if(!t||t.status==='done')return;t.status='done';t.completedDate='Today';state.history.unshift({id:'h'+Date.now(),title:t.title,detail:grade,time:'Just now',date:'Today'});save();overlayRoot.innerHTML='';showToast(`${t.title} completed`,'Undo',()=>{t.status='open';state.history=state.history.filter(h=>h.title!==t.title||h.time!=='Just now');save();render();});if(state.route==='focus')route('today');else render();}
  function showToast(message,action,callback){toastRoot.innerHTML=`<div class="toast">${esc(message)}${action?`<button id="toast-action">${esc(action)}</button>`:''}</div>`;if(action)document.getElementById('toast-action').onclick=()=>{callback?.();toastRoot.innerHTML='';};setTimeout(()=>toastRoot.innerHTML='',3200);}
  function addListItem(name){const l=listById(state.selectedList),trim=name.trim();if(!trim)return;if(l.items.some(i=>i.name.toLowerCase()===trim.toLowerCase()&&!i.checked)){showToast(`${trim} is already active`);return;}l.items.push({id:'i'+Date.now(),name:trim,quantity:'',checked:false,lastUsed:'New'});if(!l.catalog.some(i=>i.name.toLowerCase()===trim.toLowerCase()))l.catalog.unshift({name:trim,lastUsed:'New'});save();render();showToast(`${trim} added`);}
  function finishList(){const l=listById(state.selectedList),done=l.items.filter(i=>i.checked);if(!done.length){showToast('Check at least one item first');return;}done.forEach(i=>{if(!l.catalog.some(c=>c.name.toLowerCase()===i.name.toLowerCase()))l.catalog.unshift({name:i.name,lastUsed:'Just now'});});l.items=l.items.filter(i=>!i.checked);state.history.unshift({id:'h'+Date.now(),title:`${l.name} trip`,detail:`${done.length} items`,time:'Just now',date:'Today'});state.listSession=false;save();route('list-detail');showToast(`Finished ${l.name.toLowerCase()} session`);}
  function resetDemo(){localStorage.removeItem(STORAGE_KEY);state=seed();overlayRoot.innerHTML='';save();render();showToast('Demo data reset');}

  document.addEventListener('click',e=>{
    const el=e.target.closest('[data-route],[data-action],[data-complete-task],[data-start-task],[data-open-area],[data-open-room],[data-open-list],[data-open-task],[data-task-tab],[data-check-list-item],[data-add-catalog],[data-grade],[data-toggle-setting],[data-cycle-setting],[data-gemini-example],[data-quick-type],[data-add-template]');if(!el)return;
    if(el.dataset.route){overlayRoot.innerHTML='';route(el.dataset.route);return;}
    if(el.dataset.openArea){route('area-detail',{selectedArea:el.dataset.openArea});return;}
    if(el.dataset.openRoom){route('room',{selectedRoom:el.dataset.openRoom});return;}
    if(el.dataset.openList){route('list-detail',{selectedList:el.dataset.openList,listSession:false});return;}
    if(el.dataset.openTask){state.completionTarget=el.dataset.openTask;route('task-detail');return;}
    if(el.dataset.taskTab){state.taskTab=el.dataset.taskTab;save();render();return;}
    if(el.dataset.completeTask){const t=taskById(el.dataset.completeTask);if(t.grading)openGrade(t);else completeTask(t.id);return;}
    if(el.dataset.startTask){state.focusTask=el.dataset.startTask;route('focus');return;}
    if(el.dataset.checkListItem){const l=listById(state.selectedList),i=l.items.find(x=>x.id===el.dataset.checkListItem);if(i){i.checked=!i.checked;save();render();}return;}
    if(el.dataset.addCatalog){addListItem(el.dataset.addCatalog);return;}
    if(el.dataset.grade){completeTask(el.dataset.task,el.dataset.grade);return;}
    if(el.dataset.toggleSetting){state.settings[el.dataset.toggleSetting]=!state.settings[el.dataset.toggleSetting];save();render();return;}
    if(el.dataset.cycleSetting){const vals=['Gentle','Balanced','Strict'],k=el.dataset.cycleSetting;state.settings[k]=vals[(vals.indexOf(state.settings[k])+1)%vals.length];save();render();return;}
    if(el.dataset.geminiExample){state.geminiText=el.dataset.geminiExample;save();render();return;}
    if(el.dataset.quickType){overlayRoot.dataset.quickType=el.dataset.quickType;document.querySelectorAll('[data-quick-type]').forEach(x=>x.classList.toggle('active',x===el));return;}
    if(el.dataset.addTemplate){openTemplate(Number(el.dataset.addTemplate));return;}
    const action=el.dataset.action;
    if(action==='open-quick-add')openQuickAdd();
    else if(action==='close-overlay')overlayRoot.innerHTML='';
    else if(action==='another-quick'){state.quickIndex++;save();render();}
    else if(action==='back')route(state.previousRoute||'today');
    else if(action==='search')route('search');
    else if(action==='more')route('more');
    else if(action==='simulate-intervention')route('intervention');
    else if(action==='not-now')openNotNow();
    else if(action==='return-previous-app'){overlayRoot.innerHTML='';route('today');showToast('Returned to Instagram — simulation ended');}
    else if(action==='snooze-choice'){const choice=el.dataset.choice;overlayRoot.innerHTML='';route('today');showToast(choice);}
    else if(action==='exit-focus')route('today');
    else if(action==='start-room-reset'){state.resetRoom=el.dataset.room;state.resetIndex=0;state.resetQueue=state.tasks.filter(t=>t.status==='open'&&t.room===state.resetRoom&&t.type==='chore').map(t=>t.id);route('room-reset');}
    else if(action==='complete-reset-task'){const t=taskById(el.dataset.task);if(t.grading){t.status='done';t.completedDate='Today';state.history.unshift({id:'h'+Date.now(),title:t.title,detail:'Moderate',time:'Just now',date:'Today'});}else{t.status='done';t.completedDate='Today';state.history.unshift({id:'h'+Date.now(),title:t.title,detail:'Completed',time:'Just now',date:'Today'});}state.resetIndex++;save();render();}
    else if(action==='skip-reset-task'){state.resetIndex++;save();render();}
    else if(action==='toggle-list-session'){state.listSession=true;route('shopping');}
    else if(action==='finish-list-session')finishList();
    else if(action==='add-list-from-input'){addListItem(document.getElementById('list-add-input')?.value||'');}
    else if(action==='save-quick-add'){
      const title=document.getElementById('quick-add-title')?.value.trim();if(!title){showToast('Enter a title');return;}const type=overlayRoot.dataset.quickType||'task';if(type==='list'){state.selectedList='groceries';addListItem(title);overlayRoot.innerHTML='';route('list-detail');return;}const lower=title.toLowerCase(),area=lower.includes('car')?'Car':lower.includes('kitchen')||lower.includes('bathroom')?'House':document.getElementById('quick-area').value,room=lower.includes('kitchen')?'Kitchen':lower.includes('bathroom')?'Bathroom':'';state.tasks.unshift({id:'t'+Date.now(),title,type,area,room,estimate:Number(document.getElement