 data-action="toggle-list-session">${state.listSession?'Finish session':'Start shopping'}</button>`}

  function renderShopping(){const l=listById(state.selectedList), remaining=l.items.filter(i=>!i.checked), done=l.items.filter(i=>i.checked);return `${topbar(`${l.name} trip`,`${done.length} of ${l.items.length}`,false,true)}<div class="progress-card"><div class="progress-title">Shopping progress</div><div class="progress-count">${done.length} / ${l.items.length}</div><div class="progress-track"><div class="progress-value" style="width:${l.items.length?done.length/l.items.length*100:0}%"></div></div></div><div class="section-head"><div class="section-title">Remaining</div></div><div class="list">${remaining.map(i=>`<div class="task-row"><button class="check" data-check-list-item="${i.id}"></button><div><div class="task-title">${esc(i.name)}</div></div><div class="task-time">${esc(i.quantity)}</div></div>`).join('')||'<div class="notice green">Everything on the list is checked.</div>'}</div><div class="section-head"><div class="section-title">Completed</div></div><div class="list">${done.map(i=>`<div class="task-row"><button class="check done" data-check-list-item="${i.id}"></button><div><div class="task-title">${esc(i.name)}</div></div><div></div></div>`).join('')}</div><button class="button primary block" data-action="finish-list-session">Finish shopping</button>`}

  function renderIntervention(){const quicks=state.tasks.filter(t=>t.status==='open'&&t.nudge&&t.estimate<=10);const q=quicks[state.quickIndex%Math.max(1,quicks.length)];return `<div class="intervention-screen"><div class="intervention-badge">5-MINUTE RESET</div><h1 class="intervention-title">A small task is enough.</h1><p class="intervention-copy">Instagram has been open for 5 minutes. Nudge found one useful action that fits right now.</p>${q?`<div class="intervention-card"><div class="quick-meta" style="color:var(--primary)">Quick win · ${q.estimate} min</div><div class="quick-title" style="color:var(--ink)">${esc(q.title)}</div><div class="quick-subtitle" style="color:var(--muted)">${esc([q.area,q.room].filter(Boolean).join(' › '))}</div><button class="button primary block" data-start-task="${q.id}">Start task</button><div class="intervention-actions"><button class="button ghost" data-complete-task="${q.id}">Already done</button><button class="button ghost" data-action="another-quick">Different task</button></div></div>`:`<div class="intervention-card"><div class="text-center"><h3>No quick tasks available</h3><p class="muted small">Add a quick task or take a short break.</p></div><button class="button primary block" data-action="open-quick-add">Add a quick task</button></div>`}<div class="intervention-footer"><button class="text-link" data-action="not-now">Not now</button></div></div>`}

  function renderFocus(){const t=taskById(state.focusTask);if(!t)return renderToday();return `<div class="focus-screen"><p class="eyebrow">Focused task</p><h1>${esc(t.title)}</h1><p class="focus-estimate">${esc([t.area,t.room].filter(Boolean).join(' › '))} · About ${t.estimate} minutes</p><div class="timer" id="focus-timer">${formatSeconds(focusSeconds || t.estimate*60)}</div><div class="focus-actions"><button class="button primary block" data-complete-task="${t.id}">Complete</button><div class="spacer"></div><button class="button ghost block" data-action="exit-focus">Exit focus</button></div></div>`}

  function renderRoomReset(){const queue=(state.resetQueue||[]).map(taskById).filter(Boolean);const t=queue[state.resetIndex];if(!t){setTimeout(()=>{showToast(`${state.resetRoom} reset complete`);route('room');},0);return '';}return `<div class="focus-screen"><p class="eyebrow">${esc(state.resetRoom)} reset · ${state.resetIndex+1} of ${queue.length}</p><div class="reset-progress"><span style="width:${(state.resetIndex+1)/queue.length*100}%"></span></div><h1>${esc(t.title)}</h1><p class="focus-estimate">About ${t.estimate} minutes</p><div class="timer">${String(state.resetIndex+1).padStart(2,'0')} / ${String(queue.length).padStart(2,'0')}</div><div class="focus-actions"><button class="button primary block" data-action="complete-reset-task" data-task="${t.id}">Complete and continue</button><div class="spacer"></div><button class="button ghost block" data-action="skip-reset-task">Skip this task</button></div></div>`}

  function renderGemini(){if(state.geminiStage==='review')return renderGeminiReview();return `${topbar('Ask Nudge','Gemini-style prototype',true,true)}<div class="gemini-hero"><h2>Say it naturally.</h2><p>This prototype interprets common commands locally. The production version can use Gemini function calling after you approve the flow.</p></div><div class="field"><label>Your request</label><textarea id="gemini-input" class="text-area" placeholder="Add milk, eggs, and bananas to groceries, and remind me to clean the bathroom Saturday.">${esc(state.geminiText)}</textarea></div><button class="button primary block" data-action="process-gemini">Review proposed changes</button><div class="section-head" style="margin-top:22px"><div class="section-title">Try saying</div></div>${['Add milk, eggs and bananas to groceries','Create a monthly car maintenance task','What can I do in ten minutes?','Mark the kitchen lightly cleaned'].map(x=>`<div class="card clickable" data-gemini-example="${esc(x)}"><div class="card-title small">“${esc(x)}”</div></div>`).join('')}`}

  function inferGemini(text){const lower=text.toLowerCase(),changes=[];if(lower.includes('milk'))changes.push({type:'list',title:'Milk',detail:'Add to Groceries'});if(lower.includes('egg'))changes.push({type:'list',title:'Eggs',detail:'Add to Groceries'});if(lower.includes('banana'))changes.push({type:'list',title:'Bananas',detail:'Add to Groceries'});if(lower.includes('bathroom'))changes.push({type:'chore',title:'Clean bathroom',detail:lower.includes('saturday')?'Saturday · House › Bathroom':'Weekly · House › Bathroom'});if(lower.includes('car')&&lower.includes('maintenance'))changes.push({type:'task',title:'Review car maintenance',detail:'Monthly · Car'});if(!changes.length)changes.push({type:'task',title:text.trim()||'New task',detail:'Inbox · Review before saving'});return changes;}
  function renderGeminiReview(){const changes=inferGemini(state.geminiText);return `${topbar('Review changes',`${changes.length} proposed`,false,true)}<div class="notice">Nothing is saved until you confirm.</div>${changes.map((c,i)=>`<div class="review-change"><span class="plus-icon">+</span><div><div class="task-title">${esc(c.title)}</div><div class="task-sub">${esc(c.detail)}</div></div><button class="text-link" data-action="edit-proposal" data-index="${i}">Edit</button></div>`).join('')}<div class="spacer"></div><button class="button primary block" data-action="confirm-gemini">Confirm all</button><div class="spacer"></div><button class="button ghost block" data-action="cancel-gemini-review">Back to prompt</button>`}

  function renderHistory(){const groups=[...new Set(state.history.map(h=>h.date))];return `${topbar('History','Completed activity',true,true)}<div class="chips"><button class="chip active">All</button><button class="chip">Chores</button><button class="chip">Tasks</button><button class="chip">Lists</button></div><div class="spacer"></div>${groups.map(g=>`<div class="section-head"><div class="section-title">${esc(g)}</div></div>${state.history.filter(h=>h.date===g).map(h=>`<div class="history-item"><span class="history-icon">✓</span><div><div class="task-title">${esc(h.title)}</div><div class="task-sub">${esc(h.detail)}</div></div><span class="history-time">${esc(h.time)}</span></div>`).join('')}`).join('')}`}

  function renderInsights(){return `${topbar('Insights','This week',true,true)}<div class="metric-grid"><div class="metric"><strong>${state.history.length+14}</strong><span>tasks completed</span></div><div class="metric"><strong>42</strong><span>minutes redirected</span></div><div class="metric"><strong>4</strong><span>rooms maintained</span></div><div class="metric"><strong>68%</strong><span>weekly completion</span></div></div><div class="card"><div class="card-title">Completions</div><div class="bar-chart">${[3,5,2,4,6,1,4].map((v,i)=>`<div class="bar-column"><div class="bar" style="height:${v*14}px"></div><span class="bar-label">${['M','T','W','T','F','S','S'][i]}</span></div>`).join('')}</div></div><div class="card"><div class="card-title">Most maintained</div><div class="link-row"><strong>1. Kitchen</strong><span>8 completions</span></div><div class="link-row"><strong>2. Bathroom</strong><span>5 completions</span></div><div class="link-row"><strong>3. Car</strong><span>3 completions</span></div></div><div class="card"><div class="card-title">Helpful pattern</div><div class="card-sub">You complete the most quick wins between 8–10 AM. Nudge could prioritize short indoor tasks during that period.</div></div>`}

  function renderTemplates(){const templates=[['Kitchen basics','5 chores','🍽️'],['Bathroom cleaning','4 chores','🧼'],['Bedroom reset','4 chores','🛏️'],['Monthly car maintenance','6 tasks','🚗'],['Travel packing','18 list items','🧳'],['Household restock','12 list items','🧺']];return `${topbar('Templates','Start faster',true,true)}${templates.map((t,i)=>`<div class="card clickable template-card" data-add-template="${i}"><div class="list-icon">${t[2]}</div><div><div class="card-title">${t[0]}</div><div class="template-count">${t[1]}</div></div><span>${icons.chevron}</span></div>`).join('')}`}

  function renderSettings(){const s=state.settings;return `${topbar('Settings','Customize Nudge',true,true)}<div class="section-head"><div class="section-title">Interventions</div></div><div class="card"><div class="setting-row"><div><strong>Interventions</strong><small>Monitor selected apps</small></div><div class="toggle ${s.interventions?'on':''}" data-toggle-setting="interventions"></div></div><div class="setting-row"><div><strong>Strength</strong><small>Gentle, Balanced, or Strict</small></div><button class="chip" data-cycle-setting="strength">${esc(s.strength)}</button></div><div class="setting-row"><div><strong>Cooldown</strong><small>Time between redirects</small></div><span class="small muted">${esc(s.cooldown)}</span></div><div class="setting-row"><div><strong>Daily maximum</strong><small>Maximum redirect count</small></div><span class="small muted">${s.dailyMaximum}</span></div><div class="setting-row"><div><strong>Combine selected apps</strong><small>Instagram + Reddit count together</small></div><div class="toggle ${s.combineApps?'on':''}" data-toggle-setting="combineApps"></div></div></div><div class="section-head"><div class="section-title">Selected apps</div></div><div class="card"><div class="link-row"><strong>Instagram</strong><span>5 min ›</span></div><div class="link-row"><strong>YouTube</strong><span>15 min ›</span></div><div class="link-row"><strong>Reddit</strong><span>10 min ›</span></div><button class="text-link">+ Add app</button></div><div class="section-head"><div class="section-title">Lists and data</div></div><div class="card"><div class="setting-row"><div><strong>Remember completed items</strong><small>Keep reusable list history</small></div><div class="toggle ${s.rememberItems?'on':''}" data-toggle-setting="rememberItems"></div></div><div class="setting-row"><div><strong>Typing suggestions</strong><small>Show remembered items</small></div><div class="toggle ${s.suggestions?'on':''}" data-toggle-setting="suggestions"></div></div><div class="link-row" data-route="history"><strong>Completion history</strong><span>›</span></div><div class="link-row" data-route="templates"><strong>Templates</strong><span>›</span></div></div><div class="section-head"><div class="section-title">Prototype tools</div></div><div class="card"><button class="button primary block" data-action="simulate-intervention">Test direct redirect</button><div class="spacer"></div><button class="button danger block" data-action="reset-demo">Reset demo data</button></div>`}

  function renderOnboarding(){
    const step=state.onboardingStep||0;
    const steps=[
      `<div class="text-center" style="padding:34px 4px 10px"><div class="brand-mark" style="margin:0 auto 24px;width:72px;height:72px;border-radius:24px;font-size:34px">N</div><p class="eyebrow">Welcome to Nudge</p><h1 style="font-size:34px;margin:8px 0 14px;letter-spacing:-.05em">Turn scrolling into small wins.</h1><p class="muted" style="line-height:1.65">Organize chores, tasks, and reusable lists, then redirect distracting app time toward something useful.</p></div>`,
      `<div>${topbar('Choose your areas','Setup · 1 of 4',false,false)}<div class="notice green">Start with common areas. You can add or reorganize them later.</div>${state.areas.map(a=>`<div class="card"><div class="area-top"><div class="area-icon">${a.icon}</div><div><div class="card-title">${esc(a.name)}</div><div class="card-sub">${a.rooms.length?a.rooms.length+' optional subareas':'No subdivision required'}</div></div><span class="check done"></span></div></div>`).join('')}<button class="button soft block" data-action="add-area">+ Add custom area</button></div>`,
      `<div>${topbar('Select distracting apps','Setup · 2 of 4',false,false)}<div class="notice blue">Nudge only needs the selected app name and session duration. It does not read screen contents or messages.</div>${[['Instagram','5 min'],['YouTube','15 min'],['Reddit','10 min'],['TikTok','10 min']].map((x,i)=>`<div class="setting-row"><div><strong>${x[0]}</strong><small>Redirect after ${x[1]}</small></div><div class="toggle ${i<3?'on':''}"></div></div>`).join('')}</div>`,
      `<div>${topbar('Choose intervention strength','Setup · 3 of 4',false,false)}${[['Gentle','Redirect with an immediate return option.'],['Balanced','Choose an action before returning.'],['Strict','Delay repeated bypasses while keeping essential access available.']].map(x=>`<div class="card ${x[0]==='Balanced'?'clickable':''}" style="${x[0]==='Balanced'?'border-color:#7cad96;background:var(--primary-soft)':''}"><div class="card-head"><div><div class="card-title">${x[0]}</div><div class="card-sub">${x[1]}</div></div><span class="${x[0]==='Balanced'?'check done':'check'}"></span></div></div>`).join('')}<div class="notice">Android may use a heads-up notification fallback when direct opening is restricted by the device.</div></div>`,
      `<div class="text-center" style="padding:30px 6px"><div class="empty-icon" style="background:var(--primary-soft);color:var(--primary)">✓</div><p class="eyebrow">Setup complete</p><h1 style="font-size:31px;margin:8px 0 12px">Your first useful nudge is ready.</h1><p class="muted" style="line-height:1.6">You can now organize tasks, use reusable lists, and test the five-minute intervention flow.</p><div class="card" style="text-align:left;margin-top:24px"><div class="card-title">Recommended first step</div><div class="card-sub">Add three quick tasks that take less than ten minutes.</div></div></div>`
    ];
    return `<div style="min-height:100%;display:flex;flex-direction:column">${steps[step]}<div style="margin-top:auto;padding-top:20px"><div class="progress-track" style="margin-bottom:14px"><div class="progress-value" style="width:${(step+1)/steps.length*100}%"></div></div><div style="display:grid;grid-template-columns:${step?'1fr 2fr':'1fr'};gap:9px">${step?`<button class="button ghost" data-action="prev-onboarding">Back</button>`:''}<button class="button primary" data-action="${step===steps.length-1?'finish-onboarding':'next-onboarding'}">${step===steps.length-1?'Open Nudge':'Continue'}</button></div></div></div>`;
  }

  function renderWidgets(){return `${topbar('Widgets','Home-screen previews',false,true)}<div class="notice blue">These are visual prototypes. Native widgets will be implemented with Jetpack Glance.</div><div class="section-head"><div class="section-title">Next task · 4 × 2</div></div><div class="card" style="background:linear-gradient(145deg,var(--primary),var(--primary-dark));color:#fff"><div class="quick-meta">Next · 3 min</div><div class="quick-title">Take out kitchen trash</div><div class="quick-actions"><button class="button light">Start</button><button class="button white-ghost">Done</button><button class="button white-ghost">Another</button></div></div><div class="section-head"><div class="section-title">Groceries · 4 × 3</div></div><div class="card"><div class="card-head"><div class="card-title">🛒 Groceries</div><span class="count-badge">6</span></div><div class="list"><div class="task-row"><span class="check"></span><div class="task-title">Milk</div><span></span></div><div class="task-row"><span class="check"></span><div class="task-title">Eggs</div><span></span></div><div class="task-row"><span class="check"></span><div class="task-title">Bananas</div><span></span></div></div><button class="button soft block">+ Add item</button></div><div class="section-head"><div class="section-title">Quick add · 2 × 1</div></div><div class="card"><div style="display:grid;grid-template-columns:1fr 1fr;gap:9px"><button class="button soft">+ Task</button><button class="button soft">+ List item</button></div></div>`}

  function renderArchived(){return `${topbar('Archived items','Stored, not deleted',false,true)}<div class="chips"><button class="chip active">All</button><button class="chip">Tasks</button><button class="chip">Chores</button><button class="chip">Lists</button></div><div class="empty"><div class="empty-icon">□</div><h3>No archived items</h3><p>Archived areas, tasks, chores, and lists will appear here with options to restore or permanently delete them.</p></div>`}

  function renderSearch(){return `${topbar('Search','Find anything',false,true)}<div class="input-wrap"><input id="global-search" class="text-input" placeholder="Search tasks, rooms, or lists…" autofocus></div><div id="search-results"><div class="empty"><div class="empty-icon">⌕</div><h3>Search Nudge</h3><p>Find chores, tasks, areas, lists, and remembered items.</p></div></div>`}
