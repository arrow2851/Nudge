function renderAreas(){const activeAreas=state.areas;const allAttention=attentionChores(state.chores);const affected=new Set(allAttention.map(c=>c.areaId)).size;const filtered=activeAreas.filter(area=>{const status=areaStatus(areaChores(area.id));if(state.areaFilter==='attention')return status.className;if(state.areaFilter==='clear')return!status.className;return true});return`${topbar('Areas','Nudge · Style 2','',`<button class="icon-button" data-action="open-add-area" aria-label="Add area">+</button>`)}<div class="summary-banner"><strong><span class="urgent">${allAttention.length} routines</span> need attention across ${affected} areas</strong><p>Start with a specific place, then work through its due routines or browse its sections.</p></div><div class="section-block"><div class="section-heading"><h2>Your areas</h2><span>${activeAreas.length} total</span></div></div><div class="filter-row">${[['all','All'],['attention','Needs attention'],['clear','Up to date']].map(([id,label])=>`<button class="filter-chip ${state.areaFilter===id?'active':''}" data-area-filter="${id}">${label}</button>`).join('')}</div><div class="section-block" style="padding-top:0"><div class="area-list">${filtered.map(area=>{const items=areaChores(area.id),c=counts(items),status=areaStatus(items),next=nextChore(items),sections=area.sections.length,denominator=Math.max(1,items.length),progress=Math.round(c.completed/denominator*100);return`<article class="area-card"><button class="area-card-main" data-route="area/${area.id}"><span class="area-icon">${area.icon}</span><span class="area-copy"><span class="area-title-line"><strong>${esc(area.name)}</strong><i class="status-dot ${status.className}"></i></span><small>${c.total} active routine${c.total===1?'':'s'}${sections?` · ${sections} section${sections===1?'':'s'}`:' · No sections'}</small><em>${next?`${status.label} · ${esc(next.title)}`:'No routines set up yet'}</em></span><span class="area-count ${status.className}">${status.number}</span></button><div class="progress-track"><span class="progress-fill ${status.className==='overdue'?'overdue':status.className?'attention':''}" style="width:${Math.max(4,progress)}%"></span></div></article>`}).join('')||`<div class="empty-state"><div class="empty-state-icon">✓</div><h3>No matching areas</h3><p>Change the filter to see your other areas.</p></div>`}<button class="inline-add" data-action="open-add-area">+ Add another area</button></div></div>`}
function renderArea(areaId){const area=state.areas.find(a=>a.id===areaId);if(!area)return renderAreas();const items=areaChores(areaId),c=counts(items),due=attentionChores(items),sections=area.sections;return`${topbar(area.name,'Area overview','areas',`<button class="icon-button" data-action="open-add-chore" data-area-id="${area.id}" aria-label="Add chore">+</button>`)}<div class="metrics"><div class="metric attention"><strong>${c.overdue+c.due}</strong><small>Need attention</small></div><div class="metric"><strong>${items.filter(x=>urgency(x)==='upcoming').length}</strong><small>Upcoming</small></div><div class="metric"><strong>${c.completed}</strong><small>Done recently</small></div></div><div class="section-block"><div class="section-heading"><h2>Needs attention</h2><span>${due.length}</span></div>${due.length?`<div class="routine-list">${due.map(chore=>routineRow(chore,true)).join('')}</div>`:`<div class="empty-state"><div class="empty-state-icon">✓</div><h3>${esc(area.name)} is up to date</h3><p>Browse a section or add another recurring chore.</p></div>`}</div>${sections.length?`<div class="section-block"><div class="section-heading"><h2>Sections</h2><span>${sections.length}</span></div>${sections.map(section=>{const sectionItems=sectionChores(areaId,section.id),sc=counts(sectionItems),attn=sc.overdue+sc.due;return`<button class="section-card ${sectionItems.length?'':'empty'}" data-route="section/${area.id}/${section.id}"><span class="section-icon">${section.icon}</span><span><strong>${esc(section.name)}</strong><small>${sectionItems.length?`${sectionItems.length} routines${attn?` · ${attn} need attention`:' · Up to date'}`:'Not configured · Tap to add chores'}</small></span><span class="chevron">›</span></button>`}).join('')}<button class="inline-add" data-action="open-add-section" data-area-id="${area.id}">+ Add section to ${esc(area.name)}</button></div>`:''}<div class="section-block"><button class="button primary block" data-action="open-add-chore" data-area-id="${area.id}">+ Add chore to ${esc(area.name)}</button></div>`}
function renderSection(areaId,sectionId){const area=state.areas.find(a=>a.id===areaId),section=area?.sections.find(s=>s.id===sectionId);if(!area||!section)return renderArea(areaId);const all=sectionChores(areaId,sectionId);const filtered=all.filter(chore=>{const u=urgency(chore);if(state.sectionFilter==='due')return['overdue','today'].includes(u);if(state.sectionFilter==='routine')return!['as-needed','completed'].includes(u);if(state.sectionFilter==='as-needed')return u==='as-needed';if(state.sectionFilter==='completed')return u==='completed';return!chore.completed});return`${topbar(section.name,`${area.name} · Section`,`area/${area.id}`,`<button class="icon-button" data-action="open-add-chore" data-area-id="${area.id}" data-section-id="${section.id}" aria-label="Add chore">+</button>`)}<div class="detail-hero"><h2>${esc(section.name)}</h2><p>${all.length?`${attentionChores(all).length} need attention · ${all.length} total routines`:`Set up recurring chores for this part of ${area.name}.`}</p></div><div class="filter-row" style="padding-top:14px">${[['all','All'],['due','Due'],['routine','Routine'],['as-needed','As needed'],['completed','Completed']].map(([id,label])=>`<button class="filter-chip ${state.sectionFilter===id?'active':''}" data-section-filter="${id}">${label}</button>`).join('')}</div><div class="section-block" style="padding-top:0">${filtered.length?`<div class="routine-list">${filtered.map(chore=>routineRow(chore)).join('')}</div>`:`<div class="empty-state"><div class="empty-state-icon">${all.length?'✓':'+'}</div><h3>${all.length?'Nothing in this filter':'No chores here yet'}</h3><p>${all.length?'Choose another filter to see the rest.':'Add the first routine without leaving this section.'}</p><button class="button primary" data-action="open-add-chore" data-area-id="${area.id}" data-section-id="${section.id}">Add first chore</button></div>`}<button class="inline-add" data-action="open-add-chore" data-area-id="${area.id}" data-section-id="${section.id}">+ Add chore to ${esc(section.name)}</button></div>`}
function renderChore(choreId){const chore=state.chores.find(c=>c.id===choreId);if(!chore)return renderAreas();const area=state.areas.find(a=>a.id===chore.areaId),section=area?.sections.find(s=>s.id===chore.sectionId),back=section?`section/${area.id}/${section.id}`:`area/${area.id}`;return`${topbar('Chore details',area?.name||'Areas',back)}<div class="detail-hero"><h2>${esc(chore.title)}</h2><p>${esc([area?.name,section?.name].filter(Boolean).join(' › '))}</p></div><div class="detail-meta"><span class="meta-chip">↻ ${esc(chore.recurrence)}</span><span class="meta-chip">◷ ${chore.minutes} min</span><span class="meta-chip">◉ ${esc(dueText(chore))}</span>${chore.grading?'<span class="meta-chip">Light · Moderate · Deep</span>':''}</div><div class="section-block"><div class="routine-list"><div class="routine-row"><span class="section-icon" style="width:31px;height:31px;font-size:14px">↻</span><span class="routine-copy"><strong>Repeat</strong><small>${esc(chore.recurrence)}</small></span><span class="chevron">›</span></div><div class="routine-row"><span class="section-icon" style="width:31px;height:31px;font-size:14px">◉</span><span class="routine-copy"><strong>Next due</strong><small>${esc(dueText(chore))}</small></span><span class="chevron">›</span></div><div class="routine-row"><span class="section-icon" style="width:31px;height:31px;font-size:14px">▤</span><span class="routine-copy"><strong>Completion grading</strong><small>${chore.grading?'Enabled':'Off'}</small></span><span class="chevron">›</span></div></div></div><div class="detail-actions"><button class="button ghost" data-action="edit-chore" data-chore-id="${chore.id}">Edit chore</button><button class="button primary" data-action="toggle-complete" data-chore-id="${chore.id}">${chore.completed?'Reopen':'Complete'}</button><button class="button ghost" data-action="toggle-pause" data-chore-id="${chore.id}">${chore.paused?'Resume':'Pause'}</button><button class="button danger" data-action="delete-chore" data-chore-id="${chore.id}">Delete</button></div>`}
function render(){const[root,one,two]=state.route.split('/');if(root==='areas')screen.innerHTML=renderAreas();else if(root==='area')screen.innerHTML=renderArea(one);else if(root==='section')screen.innerHTML=renderSection(one,two);else if(root==='chore')screen.innerHTML=renderChore(one);else screen.innerHTML=renderAreas();screen.scrollTop=0;save()}